{
  "name": "tunein-recorder",
  "version": "0.0.1",
  "description": "Record tune in shows",
  "main": "main.js",
  "scripts": {
    "start": "cd app && npm start",
    "build": "cd app/views && bower install",
    "release": "npm run build && electron-zip-packager app --all",
    "debug": "cd app && npm run debug"
  },
  "repository": {
    "type": "git",
    "url": "totod"
  },
  "author": "Hinnerk Oetting",
  "license": "MIT",
  "devDependencies": {
    "electron-packager": "^7.0.3",
    "electron-prebuilt": "^1.2.0",
     "electron-zip-packager": "^1.0.4"
  }
}
